// This is a delay to turn on the loading spinner, to present a progress for the user.
// This is in case of the large files / slow hard drive, etc.
// If the delay is shorter than 1 second, user should not see any loading indicators.
export const DEFAULT_SPINNER_LOADING_DELAY = 1000;
